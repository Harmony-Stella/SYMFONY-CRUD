{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Liste des articles scolaires</h1>
        <table class="table table-bordered">
        <thead >
            <tr>
                <th scope="col">N°</th>
                <th scope="col">Nom</th>
                <th scope="col">Couleur</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
            <tbody>
                {% for row in tableau %}
                    <tr>
                        <td>{{ row.id }}</td>
                        <td>{{ row.nom}}</td>
                        <td>{{ row.couleur }}</td>
                        <td>
                            <a href="{{ path('modifier', {'id': row.id}) }}" class="btn btn-warning">Modifier</a>
                            <a href="{{ path('supprimer', {'id': row.id}) }}" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}