{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Liste des articles utilisateurs</h1>
        <table class="table table-bordered">
        <thead >
            <tr>
                <th scope="col">N°</th>
                <th scope="col">Nom</th>
                <th scope="col">Age</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
            <tbody>
                {% for row in tableau %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ row.nom}}</td>
                        <td>{{ row.age }}</td>
                        <td>
                            <a href="{{ path('modifier', {'id': loop.index}) }}" class="btn btn-warning">Modifier</a>
                            <a href="{{ path('supprimer', {'id': loop.index}) }}" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}